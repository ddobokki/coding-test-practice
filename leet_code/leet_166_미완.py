class Solution:
    def fractionToDecimal(self, numerator: int, denominator: int) -> str:
        num = numerator/denominator
        lenth = len(str(denominator))
        str_num = str(num)

        print(str_num)
        if len(str_num) == len(str(4/7)):
            pass



        return 's'

Solution().fractionToDecimal(4,7)
Solution().fractionToDecimal(2,3)